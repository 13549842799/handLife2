const state = {
	status: 0
}

const mutations = {
	alterStatus (state, status) {
		state.status = status
	}
}

const getters = {
	
}

const actions = {
	
}

export default {
	namespaced: true,
	state,
	mutations,
	getters,
	actions
}